const map = require('./lists/map');
const filter = require('./lists/filter');
const movies = require('./data/movies.json');
const bookmarks = require('./data/bookmarks.json');

console.log(result);
