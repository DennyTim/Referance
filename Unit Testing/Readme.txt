$ npm init
$ npm install --global mocha				// установка модуля дла автоматической работы тестов - основа
$ npm install chai							// дополнительные фу-и для проверок

let assert = require("chai").assert;        // подключаем доп фу-ал стиля чай
describe(arg, callback)                     // формируем блок кода мока (arg является название фу-и), внутри тела проверки
$ mocha test                                // выполняем проверки
$ mocha --watch                             // автоматическая слежка за файлами которые меняються